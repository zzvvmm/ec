$("#cart-total-money").html(
  "<%= number_to_currency_format total_of_money %>"
);
$("#item-product-allprice_<%=item[:product].id%>").html(
  "<%= number_to_currency_format line_total__price(item) %>"
);
